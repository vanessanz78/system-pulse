{
  "name": "@system-pulse/desktop",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dev:web": "vite --host 127.0.0.1 --port 1420",
    "build:web": "vite build",
    "check": "tsc --noEmit",
    "prepare:icons": "python scripts/generate-icons.py"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.3",
    "typescript": "6.0.3",
    "vite": "8.1.0"
  }
}
